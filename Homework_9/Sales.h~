#ifdef _SALES_H
#define _SALES_H

#include <iostream>

class Sales
{
	private:
		std::string mEmployeeName;
		int mEmployeeNumber;
		int mPassword = 1234;
	public:
		Sales(std::string aEmployeeName, int aEmployeeNumber);
		std::string mEmployee_Info();
};

#endif
